import { StyleSheet, Text, View } from 'react-native';
import React from 'react';

const DetalleRegistroScreen = () => {
	return (
		<View>
			<Text>DetalleRegistroScreen</Text>
		</View>
	);
};

export default DetalleRegistroScreen;

const styles = StyleSheet.create({});
